# AGENTS.md

> 本文件规定 JO-引号转换在 AI 辅助开发和日常维护中的长期约束。处理任务前应先阅读本文件，并以当前代码、配置、测试和用户明确要求为准。

## 1. 项目事实

- 项目名称：JO-引号转换。
- 当前版本：`0.0.3+3`。
- 作者：GitHub 用户 `JO-Beacon`。
- 技术栈：Flutter / Dart。
- 当前维护平台：Windows、Android。
- 暂不维护 Web、iOS、macOS 和 Linux；除非用户明确提出，不为这些平台增加代码、配置或构建产物。
- Android 包名固定为 `io.github.jobeacon.joquoteconverter`。修改 Android 配置时，必须保持 Gradle `namespace`、`applicationId` 和 `MainActivity` 包声明一致。
- 项目源代码采用 Apache License 2.0；内置思源黑体采用 SIL Open Font License 1.1。
- 项目不接受 Pull Request，只接受 Issue。安全漏洞应按照 `SECURITY.md` 私密报告。

## 2. 产品边界

- 本程序是本地英文引号转中文引号工具，只做英转中，不增加中转英功能，除非用户以后明确改变产品方向。
- 文本转换、草稿和历史记录均在本地处理，不上传用户文本，不依赖账号或后端服务。
- 启发式判断与 Markdown 代码排除是两个相互独立的设置，不得让其中一个开关隐式改变另一个开关的行为。
- 启发式判断用于尽量识别英文缩写、所有格、长度单位和年代缩写等歧义写法，但不能宣称结果绝对可靠。
- 关闭启发式判断后，英文单双引号分别采用左、右、左、右的机械配对方式。
- 开启 Markdown 代码排除后，应保留围栏代码块和行内代码中的原始引号。
- 不重复转换已经存在的中文弯引号。

## 3. 界面与交互约束

- 所有用户可见文案默认使用中文，并保持简洁、明确。
- 应用使用内置思源黑体，不回退为默认字体作为正常设计方案。
- 保留跟随系统、浅色和深色三种外观模式。
- 保留红、黄、绿、蓝、紫、灰六套配色，默认配色为灰色。
- 设置界面承载 Markdown 排除、启发式判断、外观模式、主题、版本、作者和许可证信息。
- 桌面宽屏工作区使用左右分栏，窄屏使用上下布局；新增 UI 时必须同时检查桌面和窄屏布局。
- “转换”和“清空”按钮位于“原文”标题右侧，采用紧凑扁平尺寸；“清空”使用危险操作样式并二次确认。
- “复制”按钮位于“转换结果”标题右侧，并与原文区域按钮保持一致的视觉规格。
- 历史记录支持恢复和删除。恢复到非空工作区前必须二次确认，删除历史记录前必须二次确认。
- 不添加只有外观、没有实际行为的控件。暂未实现的功能必须明确保持在约定范围内。

## 4. 数据与兼容性

- 原文、转换结果及相关工作区设置应实时保存，应用正常关闭、进入后台或退出时应补写当前状态。
- 每次用户点击“转换”后，应将原文、转换结果和时间写入 SQLite 历史记录。
- 修改数据库结构、首选项键名、应用标识或数据目录前，必须先检查现有用户数据兼容性。
- Windows 当前数据目录归属 `JO-Beacon/JO-引号转换`。不得重新使用旧的 `com.localtools` 目录作为新数据目录。
- 涉及存储位置或格式变化时，必须提供可验证的数据迁移方案；不得静默丢弃、覆盖或删除用户草稿与历史记录。
- 未经用户明确授权，不得删除旧数据。执行已获授权的数据清理前，仍应确认目标路径准确且迁移已经成功。
- 当前历史记录不做压缩。除非用户明确要求，不预先增加压缩层、归档层或存储限额。

## 5. 开发方式

- 与用户沟通使用中文，结论应基于当前代码、配置、测试结果或可复现现象，不猜测项目状态。
- 优先做范围最小、可验证的修改，不顺带重构无关代码，也不为假设中的未来需求预设抽象。
- 修复问题时先定位根因和触发条件；不要用吞错、静默降级或伪成功状态掩盖错误。
- 修改前检查相关代码和已有测试。发现工作区中存在无关改动时保留它们，不擅自还原。
- 用户只要求分析、解释或审查时，不应擅自修改代码。
- 不手工修改 `.dart_tool/`、`build/` 等生成目录中的内容。
- 不提交真实密钥、令牌、签名文件或个人环境配置。
- 用户明确要求“不 dist”时，不创建或更新 `dist/` 中的产物。当前默认规则是：未经明确要求，不更新 `dist/`。
- 用户明确要求“不编译安卓”时，不运行 APK、App Bundle 或其他 Android 构建。当前默认规则是：未经明确要求，不构建 Android。
- 不因验证方便而扩大公开 API、改变生产代码职责或破坏已有持久化格式。

## 6. 验证要求

- 修改 Dart 文件后，对变更文件运行 `dart format`。
- 常规完成检查至少包括：

```powershell
dart analyze
flutter test
```

- 若改动范围很窄，可以运行相关测试子集，但交付时必须说明未运行完整测试的原因和剩余风险。
- 修改 `pubspec.yaml` 或依赖后，先运行 `flutter pub get`，再执行分析和相关测试。
- 修改 Android 平台文件时，应检查包名三处一致；除非用户明确授权，不以 Android 编译作为验证手段。
- 修改持久化逻辑时，至少覆盖：新数据写入、已有数据读取、空数据、应用重启恢复，以及需要迁移时的旧数据迁移。
- 修改引号转换逻辑时，测试至少覆盖：双引号、单引号、嵌套或歧义输入、启发式开关两种状态、Markdown 排除开关两种状态，以及两个开关的组合。
- 修改历史记录时，测试至少覆盖：转换触发写入、最新优先展示、恢复、非空工作区确认、删除和删除确认。
- 交付前检查变更差异，确认没有生成物、无关文件、用户数据或许可证内容被意外修改。

## 7. 文档与发布

- 用户可见功能、数据行为、支持平台或运行方式发生变化时，同步更新 `README.md`。
- 发布版本的重要变化记录在 `CHANGELOG.md`，版本号以 `pubspec.yaml` 为准。
- 新增第三方依赖或资源时，检查其许可证兼容性，并在适当位置补充声明。
- GitHub Issue 模板继续区分错误报告和功能建议。
- 未经用户明确要求，不初始化 Git、不创建 GitHub 仓库、不提交、不推送，也不制作发布包。

## 8. 完成交付

每次完成代码或配置修改后，应向用户说明：

- 改了什么。
- 运行了哪些检查，以及结果如何。
- 哪些构建或测试没有运行及其原因。
- 是否存在数据兼容、平台覆盖或发布方面的剩余风险。
