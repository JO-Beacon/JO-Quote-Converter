# 安全策略

## 支持的版本

JO-引号转换目前处于早期开发阶段，仅维护最新发布版本。

| 版本 | 安全更新 |
| --- | --- |
| `0.0.1+1` | 支持 |
| 更早版本 | 不支持 |

发布新版本后，本表会随版本支持范围更新。

## 报告安全漏洞

请优先使用本仓库 GitHub `Security` 页面中的 **Report a vulnerability** 私密报告入口，不要通过公开 Issue 披露尚未修复的安全漏洞。

报告中请尽量包含：

- 受影响的平台和应用版本；
- 漏洞类型、影响范围和可能后果；
- 可重复执行的复现步骤或最小验证样例；
- 已知的利用条件；
- 建议的修复方式或缓解措施（如有）。

请勿提交真实个人文本、账号、令牌、密钥、签名文件或其他敏感数据。需要提供样例时，请使用脱敏或虚构内容。

如果 GitHub 私密漏洞报告入口暂不可用，请仅创建一个不含漏洞细节的公开 Issue，说明需要建立私密联系；维护者确认私密沟通方式后，再提供完整报告。

## 处理方式

维护者会在看到报告后尽快确认是否能够复现，并根据影响范围安排修复。确认漏洞后，通常会按以下顺序处理：

1. 评估受影响版本、平台和数据范围；
2. 准备并验证修复；
3. 发布修复版本和必要的升级说明；
4. 在不会增加用户风险的前提下公开漏洞信息。

修复发布前，请避免公开复现细节或可直接利用的示例，为用户保留合理的升级时间。

## 安全范围

以下问题适合作为安全漏洞私密报告：

- 本地草稿或历史记录被未授权读取、覆盖或删除；
- 路径处理、数据迁移或 SQLite 操作导致越权访问；
- 构建产物、配置或日志意外泄露敏感信息；
- 可导致任意代码执行、命令执行或文件写入的问题；
- 第三方依赖中能够实际影响 JO-引号转换的已知漏洞。

普通转换结果错误、界面异常、性能问题和功能建议不属于私密安全报告范围，请使用对应的 GitHub Issue 模板。
